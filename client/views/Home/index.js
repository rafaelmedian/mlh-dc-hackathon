import React from 'react';
import PropTypes from 'prop-types';

const Home = props => {
  return (
    <div>
      Home
    </div>
  );
};

Home.propTypes = {
  children: PropTypes.node,
};

Home.defaultProps = {};

export default Home;